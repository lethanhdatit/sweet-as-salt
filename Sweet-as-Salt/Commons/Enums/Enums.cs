using System;
using System.Collections.Generic;
using System.Text;

namespace Sweet_as_Salt.Common
{
    public enum BaseEnumStatus : byte
    {
       Active = 1,
       Inactive = 2
    }
    public enum UserTypes : byte
    {
        GUEST = 1,
        MEMBER = 2
    }
}
